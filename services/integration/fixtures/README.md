# Integration Test Fixtures

Place the following PDF files here before running integration tests:

- `neumann_hydraulik.pdf` – Machine maintenance document with hydraulic pressure data
- `gw_cnc_ruest.pdf` – CNC Ruestmappe with G-code blocks
- `wl_vorschrift.pdf` – Wiener Linien Vorschrift document with § references

Tests will be skipped for missing fixtures.
