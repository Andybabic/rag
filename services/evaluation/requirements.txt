# Pull torch from the PyTorch CPU-wheel index — sentence-transformers
# transitively needs torch, but the default PyPI wheel ships with CUDA libs
# (~4 GB of nvidia-* + triton). The +cpu local-version pin disambiguates so
# pip picks the CPU wheel even though the same x.y.z exists on PyPI.
# Bump this pin when you upgrade torch.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.11.0+cpu

fastapi>=0.100
uvicorn[standard]>=0.23
pydantic-settings>=2.0
httpx>=0.24
asyncpg>=0.29
ruff>=0.4
pytest>=7.0
pytest-asyncio>=0.21
anyio
rank-bm25>=0.2.2
sentence-transformers>=2.7.0
